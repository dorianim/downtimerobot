on:
  workflow_dispatch:

name: Run Downtimerobot

jobs:
  downtimerobot:
    name: Downtimerobot
    runs-on: ubuntu-latest
    steps:
      - name: run
        uses: dorianim/downtimerobot-action@v0.0.2
